


function main {
	let src = Integer{ value: 32 } ();
	let dst = Integer{ value: 40 } ();
	let inc = Integer{ value: 10 } ();

	let dstPtr = &dst;
	let incPtr = &dst;
	src.inject<src, dst, inc, 49>(dstPtr, incPtr);
	src.inject<src, dst, inc, 49>(dstPtr, incPtr);

	return dst.value;
}
